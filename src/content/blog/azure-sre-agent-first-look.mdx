---
title: "An Hour with Azure SRE Agent"
heroImage: ../../assets/blog/azure-sre-agent-first-look.webp
heroAlt: "Editorial illustration representing Azure."
description: "What Azure SRE Agent is, what setup actually looks like, and what happened when I pointed it at my own repo, my Application Insights telemetry across Dataverse, Copilot Studio, and Foundry, and then asked it for a health check on everything at once. Plus the five switches worth flipping that you don't get turned on by default, and the 403 that stands between you and running the whole thing from your terminal."
pubDate: 2026-08-14
updatedDate: 2026-08-16
tags: ["Azure", "Azure SRE Agent", "Application Insights", "Copilot Studio", "Dataverse", "Microsoft Foundry", "GitHub", "Security", "DevOps"]
draft: false
featured: false
---

import Screenshot from "@/components/Screenshot.astro";
import operationsHub from "@/assets/blog/azure-sre-agent-first-look/operations-hub-overview.png";
import automation from "@/assets/blog/azure-sre-agent-first-look/automation-create-trigger.png";
import toolPermissions from "@/assets/blog/azure-sre-agent-first-look/connector-tool-permissions.png";
import codeReviewThread from "@/assets/blog/azure-sre-agent-first-look/code-review-thread.png";
import parallelExploration from "@/assets/blog/azure-sre-agent-first-look/parallel-exploration-subagents.png";
import codeReviewFindings from "@/assets/blog/azure-sre-agent-first-look/code-review-findings.png";
import agentConsumption from "@/assets/blog/azure-sre-agent-first-look/agent-consumption-aau.png";
import quickstartAgentList from "@/assets/blog/azure-sre-agent-first-look/quickstart-agent-list.png";
import quickstartSetup from "@/assets/blog/azure-sre-agent-first-look/quickstart-setup-checklist.png";
import quickstartCodeAccess from "@/assets/blog/azure-sre-agent-first-look/quickstart-code-access.png";
import builtinTools from "@/assets/blog/azure-sre-agent-first-look/builtin-tools-permissions.png";
import pluginsMarketplace from "@/assets/blog/azure-sre-agent-first-look/plugins-add-marketplace.png";
import demoRunning from "@/assets/blog/azure-sre-agent-first-look/demo-investigation-running.png";
import demoFindings from "@/assets/blog/azure-sre-agent-first-look/demo-unhealthy-findings.png";
import demoPriority from "@/assets/blog/azure-sre-agent-first-look/demo-priority-table.png";
import liveReport from "@/assets/blog/azure-sre-agent-first-look/live-report-dashboard.png";
import incidentsOnboarding from "@/assets/blog/azure-sre-agent-first-look/incidents-onboarding.png";
import agentCanvas from "@/assets/blog/azure-sre-agent-first-look/agent-canvas-subagents.png";

Azure SRE Agent is Microsoft's operations agent for the work that eats an
on-call rotation: triaging incidents, checking whether services are actually
healthy, and running the runbook nobody has updated since the last reorg. It
went generally available in March 2026, and if you haven't tried it yet, the
short version is that it does the first twenty minutes of an investigation for
you and shows its work.

I gave it an hour. In that hour it reviewed one of my repos and handed back
findings that went well past the usual security scan into performance,
accessibility, and UX. Then I asked it what my Application Insights instances
were actually recording, and it surfaced unhealthy telemetry from Dataverse,
Copilot Studio, a couple of app servers, and a Foundry agent in a single pass.

Here's what it is, what it can do, and where I'd be careful with it.

## What Azure SRE Agent is

It connects your observability tools, incident platforms, and source
repositories into one workflow. Instead of hopping between a dashboard,
PagerDuty, and Slack when something breaks, you get a single investigation with
the "what changed, what's affected, what to do next" already worked out.

<Screenshot
  src={operationsHub}
  alt="Azure SRE Agent Operations Hub overview showing a setup checklist with Code, Logs, and Azure resources connected, a daily volume by source chart, an empty pending actions panel, and a system health panel reporting a healthy agent process and two healthy connectors"
  caption="The Operations Hub is the front door: what's connected, what's pending your approval, and whether the agent itself is healthy."
/>

It ships with six built-in subagents — Explore, Plan, CodeReview, Bash,
Verification, and GeneralPurpose — plus extension points for custom skills,
Python tools, MCP server connections, and event-triggered hooks. Microsoft has
since added native Application Insights and Log Analytics connectors that
handle the RBAC wiring for you, so telemetry is queryable the moment you
connect a resource instead of after a separate permissions project.

Those hooks aren't something you script yourself. The Automation blade takes
two kinds of trigger: a scheduled task that runs a check on a cadence you set,
or an HTTP trigger you point an alert rule or webhook at, so an Azure Monitor
alert can start the investigation before anyone opens a laptop.

<Screenshot
  src={automation}
  alt="Azure SRE Agent Automation blade with the Create menu expanded, showing the two options: Scheduled task and HTTP trigger"
  caption="Automation gives you two entry points — a scheduled check or an HTTP trigger fired by an alert or webhook."
/>

Nothing it proposes deploys without a human approving it first. That
distinction matters more than it sounds like it should: this is an agent that
recommends and drafts, not one that reboots your production database because
it decided that was a good idea at 3 a.m. Microsoft's own writeup on the
rollout describes the mechanics behind that guardrail: the agent explains what
it intends to do and why before acting, every interaction produces a full
audit trail, routine operations run autonomously, and anything flagged as
high-impact pauses for in-workflow sign-off. There's a network layer underneath
all of that as well, which I get into further down. See
[Rethinking cloud operations with Azure SRE Agent](https://techcommunity.microsoft.com/blog/appsonazureblog/a-paradigm-shift-in-cloud-operations-with-azure-sre-agent/4533244)
for the full picture.

You set that approval model before anything runs. When you wire up a
connector, every tool it exposes gets its own permission: Allow lets the agent
call it unattended, Ask forces a prompt each time. The Microsoft Teams
connector I set up exposes 71 tools, and they're individually switchable
rather than a single all-or-nothing grant. If you're rolling this out to a
team, that page is where you decide what the agent can do on its own and what
it has to ask about.

<Screenshot
  src={toolPermissions}
  alt="Azure SRE Agent connector setup wizard on the Configure tools step, showing 71 of 71 tools selected with a per-tool parameter policy toggle and an Allow or Ask permission control for each one"
  caption="Per-tool permissions during connector setup — Allow for unattended calls, Ask to force a prompt."
/>

Those extension points aren't purely theoretical. Microsoft added a plugin
marketplace so you're not building every skill from zero: it lets you browse
and install community and internal skills and MCP integrations from
GitHub-hosted repositories, including private repos and GitHub Enterprise.
Worth knowing before you go looking for it: a new agent has no marketplace
registered at all, and the Plugins blade sits empty until you add one. The
Add marketplace dialog offers two well-known ones as presets: Azure's own
[Azure SRE Agent Plugins](https://github.com/Azure/sre-agent-plugins) and
Anthropic's [Claude Plugins](https://github.com/anthropics/claude-plugins-official).
You can also point it at any other repo by `owner/repo`, a github.com URL,
or a `tenant.ghe.com` URL. Every install is pinned to a specific git commit,
so an upstream change to a skill you installed months ago doesn't quietly
alter your agent's behavior until you choose to update it.

<Screenshot
  src={pluginsMarketplace}
  alt="Add marketplace or plugin dialog in Azure SRE Agent with the well-known marketplaces dropdown open, showing two options: Azure SRE Agent Plugins (Azure/sre-agent-plugins) and Claude Plugins (anthropics/claude-plugins-official), above a field for a marketplace URL or owner/repo"
  caption="Nothing is registered by default. The two well-known marketplaces are presets you pick, not defaults you inherit."
/>


One thing worth flagging if you're the one managing this for a team: private
marketplaces store a single credential — an OAuth token, a personal access
token, or a GitHub App — at the marketplace level, and every install from
that marketplace uses it. SRE Agent checks that the stored credential can
read the repo, not that the person installing the plugin personally has
access to it. Anyone with the Author or Administrator role on the agent can
install from any registered marketplace, regardless of their own GitHub
permissions. If you need to lock that down, the control point is the agent's
RBAC roles, not GitHub repo access.

## What setup actually looks like

An agent is an Azure resource, so it starts where every Azure resource
starts: a subscription, a resource group, and a region. You create it from
the agent list at [sre.azure.com](https://sre.azure.com), and once it exists
it shows up alongside any others you have, region and all. Mine live in two
different regions because I built them at different times and never went
back to tidy that up.

<Screenshot
  src={quickstartAgentList}
  alt="Azure SRE Agent portal agent list showing two agents named sre-doc and the-sre-fixer with their subscription blurred, resource group appinsights, and regions North Central US and East US 2, above a Create agent button"
  caption="The agent list. Each one is a real Azure resource in a subscription, resource group, and region."
/>

Open a new agent and it tells you what it still needs. The setup strip runs
across the top of the chat and tracks five things: Incidents, Knowledge
files, Code, Logs, and Azure resources. Green check means connected, hollow
circle means the agent is flying without it. You don't have to finish all
five before asking a question. I ran useful investigations with Incidents
and Knowledge files still unconfigured. But the strip is honest about what
it can't see yet.

<Screenshot
  src={quickstartSetup}
  alt="Azure SRE Agent setup checklist expanded, showing Incidents and Knowledge files as not configured, and Code with one repository, Logs with one log provider, and Azure resources with one Azure resource all marked configured"
  caption="The setup strip, expanded. Two unconfigured, three connected, and it works fine with the three."
/>

Code Access is where the repo connection lives, and it's per-domain rather
than per-repo: you authorize github.com or your GitHub Enterprise host once,
then add repositories under it. GitHub, Azure DevOps, and GitLab each get
their own tab. Once a repo syncs, you get a Ready status and a timestamp, so
you can tell at a glance whether the agent is reviewing today's code or a
snapshot from last week.

<Screenshot
  src={quickstartCodeAccess}
  alt="Code Access Configuration page in Azure SRE Agent with tabs for GitHub, Azure DevOps, and GitLab, showing a connected github.com domain using OAuth and one repository named PortalofPortals with a Ready status and a sync timestamp"
  caption="Code Access. Authorize the domain once, then add repos under it. The sync time tells you how fresh the review is."
/>

The permissions story goes deeper than the per-connector toggles. The Tools
blade lists every built-in tool the agent can call. Fifty of them on my
agent, 35 active, and each one has both an on/off switch and an
Allow-or-Ask permission. A few are locked on and can't be disabled. They're
grouped by category, so you can reason about them in batches instead of one
at a time: System, DevOps, Workspace Operation, and so on. There are
separate tabs for MCP servers, custom tools, advanced permissions, and
approval expiration.

<Screenshot
  src={builtinTools}
  alt="Tools page in Azure SRE Agent showing 35 of 50 active built-in tools in a table with On/Off toggles, Allow or Ask permission controls, category, and description columns, plus tabs for MCP servers and services, custom tools, advanced permissions, and approval expiration"
  caption="Every built-in tool, individually switchable, each with its own Allow or Ask setting. Some are locked on."
/>

If you're rolling this out to a team, this page and the connector one are
where the actual policy lives. Everything else is configuration; these two
decide what the agent is allowed to do when nobody's watching.

## Where its traffic can actually go

Those two pages decide what the agent can do. Neither of them decides where its
traffic ends up. By default the answer is "anywhere on the public internet,"
which is fine for a test agent and a real problem for one you've just handed
read access across production.

[VNet integration](https://techcommunity.microsoft.com/blog/appsonazureblog/vnet-integration-for-azure-sre-agent-preview/4524287)
landed in preview in June, and it's the piece I'd read before putting this
anywhere sensitive. The argument for it is one your security team already makes
about every other workload: a component's network access shouldn't depend on
the component behaving correctly. The agent reads logs, code, and configuration
by design, so open egress gives that data a route out. It also reasons over
text it didn't write, which is the textbook prompt injection surface. A
boundary enforced in the network holds whether or not the model spots the trick.

You pick from three egress modes under Settings > Workspace configuration >
Networking:

- **Unrestricted.** The default. Any endpoint, no restrictions.
- **Limited.** Deny all, allow an explicit list of hosts. Host-level control
  without standing up a VNet.
- **Azure VNet.** Non-platform outbound traffic routes through a delegated
  subnet in your network, with your NSG rules, firewall policies, custom DNS,
  and network logging applied to it.

In Azure VNet mode the agent is just another workload on that subnet. It
reaches what the subnet can reach and nothing else: databases behind private
endpoints, internal services, a Log Analytics workspace with public access
disabled, anything on-premises you've connected over ExpressRoute or VPN.
During a real incident most of what you want the agent reading is private
anyway, so this constrains less than it sounds like it would.

### The subnet has a few requirements

The subnet has to be dedicated, sit in the same region as the agent, and be
delegated to `Microsoft.App/environments` — the Container Apps delegation,
which is not the one you'd guess. You need Network Contributor on the subnet
(or any role carrying `Microsoft.Network/virtualNetworks/subnets/join/action`)
plus SRE Agent Administrator on the agent resource.

On size, the docs say /28 or larger and suggest /26 for a fleet, and the
announcement post mentions /27. A /27 covers all of that with room to grow.

Two bits of friction worth knowing before you commit: once a VNet is connected,
the Unrestricted and Limited cards grey out, so changing modes means
disconnecting first. Changing the subnet means disconnect and reconnect too.

### Link the private DNS zones, or you'll debug this at the wrong layer

Connecting a VNet enables "Use VNet's private DNS" automatically, but that only
does something if the relevant Azure Private DNS zones are actually linked to
the VNet: `privatelink.ods.opinsights.azure.com` for Log Analytics,
`privatelink.vaultcore.azure.net` for Key Vault, and so on for anything else
behind a private endpoint. Skip that step and the agent either falls back to
public endpoints or fails to connect.

That matters more than usual because of how the agent reports failures. When
the network blocks a call, it gets the same error any workload on that subnet
would get, notes it in the investigation output, and carries on with whatever
it can still reach. Correct behavior, but it means an investigation can come
back confident and incomplete at the same time. Read the failures, not just the
findings.

### The bypass toggles are the part to govern

Some traffic never touches your VNet regardless. Platform services —
orchestration, model endpoints, telemetry — always route through Microsoft's
managed infrastructure and aren't configurable.

Below that sits an "On the infra network" section with toggles for package
registries (PyPI, npm, NuGet, apt), code repositories (GitHub, GitHub
Enterprise, Azure DevOps), remote MCP servers, and a free-form list of extra
hostnames. Anything switched on there skips your VNet entirely, so your NSG
rules and firewall policy never see it.

The reason they exist is practical rather than lazy. None of those services map
to an Azure service tag, and they run on large, frequently changing IP ranges.
If your firewall filters by IP, maintaining that list is a standing chore, and
the day it falls behind an investigation stalls on a package download instead
of the incident. Microsoft's own framing is that the toggles are transitional:
the destination is FQDN-based egress filtering in your network, at which point
you move those categories back through the VNet.

Two things narrow the gap in the meantime. The Packages tab lets you preinstall
pip and .NET CLI tool packages into the sandbox image, which removes a reason
to leave the registry toggle on at all. And you can apply Azure Policy to
restrict or disable the toggles, so no operator can quietly widen the agent's
reach.

### What the preview still doesn't cover

Two limitations, and the second one is easy to miss. It's egress only, so
reaching the agent privately from inside your network isn't part of this
preview. And connector traffic doesn't route through the VNet at all. That
Teams connector with 71 tools goes over the public internet whether or not
you're in Azure VNet mode, and the same is true of anything else you wire in
through Connectors. If your egress story assumes connectors sit inside the
boundary, they don't yet.

## The code review went further than I expected

I pointed the CodeReview subagent at one of my project repos expecting the
usual: dependency CVEs, a couple of hardcoded secrets I'd meant to rotate out,
maybe an injection risk in a query I'd written too quickly. I got that. What
I didn't expect was the rest of the list.

<Screenshot
  src={codeReviewThread}
  alt="Azure SRE Agent chat thread titled portalofportals repository code recommendations, showing a parallel exploration step with two completed subagents, Review and Explore, followed by recommendations ordered by severity"
  caption="One question, one thread. The agent picks its own subagents and shows each step as it runs."
/>

Before it answered, it fanned out. A CodeReview subagent took the security and
convention pass while an Explore subagent mapped the repo structure, both
running at once, both reporting back before the agent synthesized anything.
That structure is visible in the thread rather than hidden behind a spinner,
which matters when you want to know whether a finding came from reading the
code or from guessing at it.

<Screenshot
  src={parallelExploration}
  alt="Expanded parallel exploration card in Azure SRE Agent showing final results from two subagents side by side: a CodeReview agent summarizing a full repository review of a Next.js and React application, and an Explore agent reporting 813 route files across the API tree"
  caption="Both subagents' final results, side by side — including the Explore agent counting 813 route files."
/>

It flagged a performance issue in how I was paginating a large result set,
an accessibility gap where a custom form control had no accessible label, and
a UX inconsistency between two error messages that told users two different
things for the same failure. None of that is exotic advice on its own — any
good reviewer would catch it. What stood out was getting security, performance,
accessibility, and UX findings back in the same pass, against my own code, in
minutes instead of a scheduled review cycle.

The findings came back ordered by severity, each one pointing at the specific
file and line rather than describing the problem in the abstract. A pre-release
`next-auth` sitting under 800-plus API routes, unguarded `console.log` calls
leaking partial API keys and decoded token claims into production logs,
hardcoded hex colors in six chart components that bypassed the theme tokens
already defined in `globals.css`. Every one of those was checkable in under a
minute because it told me where to look.

<Screenshot
  src={codeReviewFindings}
  alt="Azure SRE Agent code recommendations listing a pre-release next-auth dependency, unguarded console.log calls in API routes leaking partial API keys and decoded token claims, and a table of six chart components with hardcoded hex colors that bypass the theme system"
  caption="Findings with file paths and a suggested fix attached — not a generic 'consider reviewing your logging' note."
/>

If you run a solo shop or a small team without a dedicated accessibility or
performance reviewer, that's the gap this fills. Not a replacement for a real
code review culture, but a much earlier catch than "we'll get to it before
launch."

## Then it checked what my telemetry was actually saying

The second half of the hour was the one that changed how I think about this
tool. I have Application Insights wired up across several different pieces of
my environment: Dataverse, a Copilot Studio agent, a couple of app servers,
and a Foundry-hosted agent. Each of those emits its own telemetry, and
normally checking all of it means opening separate blades, running separate
KQL queries, and manually correlating anything that looks off.

Once Azure SRE Agent had read access to those Application Insights resources,
it queried requests, dependencies, and exceptions across all of them and
called out the unhealthy signals directly: a latency trend on one app server
that had been quietly climbing, an elevated exception rate coming out of the
Copilot Studio agent, and a dependency call from the Foundry agent that was
failing more often than it should. It correlated pieces of that with recent
changes and proposed what to look at first, rather than handing me four
dashboards and telling me to figure it out myself.

That's the difference between monitoring and triage. Monitoring tells you
something is happening. Triage tells you what to do about it, and where to
start.

## One prompt, one health check

Since writing the first version of this post I went back and gave the agent
a deliberately lazy prompt, the kind you'd actually type at 2 a.m.: give me
a health check across everything you're connected to, list the Azure
resources you manage, query Application Insights for the last 24 hours, and
tell me what looks unhealthy and what you'd investigate first.

It took 147 seconds. The interesting part is what it showed while it worked.
It announced it was gathering everything in parallel, then ran the actual
commands in the open: `az graph query` calls against Resource Graph, KQL
against the requests, exceptions, and dependencies tables in Application
Insights, with each one labeled and each one marked complete as it landed.
The Resource Graph calls carried a **Medium risk** tag. You can read the
exact query it ran and copy it out.

<Screenshot
  src={demoRunning}
  alt="Azure SRE Agent thread mid-investigation showing a step labeled Performing comprehensive system health diagnostics, several completed Monitor Resource Log Query steps, and two Executing graph query cards tagged Medium risk with the raw az graph query commands visible"
  caption="Mid-run. The queries aren't summarized for you. They're printed, tagged with a risk level, and copyable."
/>

The answer opened with an inventory, then a section headed "What's Unhealthy
/ Noteworthy." All 13 VMs deallocated, including a 10-node compute fleet.
Seven of ten web apps stopped, two of which still had failure-anomaly alert
rules pointed at them, which, as it noted, is an odd combination if
they're meant to be off.

<Screenshot
  src={demoFindings}
  alt="Azure SRE Agent health check results listing resource inventory counts, then findings that all 13 VMs are deallocated, that 7 of 10 web apps are stopped with running and stopped apps in a table, and that one Application Insights resource shows a 100 percent request failure rate marked benign"
  caption="Inventory first, then what's actually wrong, including the stopped apps that still have alert rules pointed at them."
/>

The finding I keep coming back to is the third one. One Application Insights
resource showed a 100 percent request failure rate over 24 hours, which
looks alarming until you read the next line: four requests, all 404s, all
for `GET /robots933456.txt`. That's the App Service internal health probe
hitting a container. The agent labeled it benign, explained why, and then
made the point that actually mattered: near-zero real traffic means either
nobody is using the app or the SDK isn't wired up correctly.

A dashboard would have drawn that as a red 100 percent and left me to work
out the rest. Catching a false positive and then finding the real problem
hiding behind it is the part that's hard to automate, and it's the part it
did well.

It closed with a priority table, P1 through P4, each row carrying a reason
rather than just a severity, and a separate list of what looked healthy so I
wasn't left guessing about the things it didn't mention.

<Screenshot
  src={demoPriority}
  alt="Azure SRE Agent investigation priority table with rows P1 through P4 listing the issue and the reason for its priority, followed by a What Looks Healthy list naming running web apps with their pricing tiers, enabled logic apps, provisioned Cosmos DB accounts, key vaults, and API Management instances"
  caption="P1 through P4 with reasoning attached, plus an explicit healthy list so silence doesn't get mistaken for a pass."
/>

Its closing line was the right one: investigate the telemetry gap first,
because it couldn't tell me whether my main app was healthy when there was
no signal coming out of it. That's a better answer than a fake all-clear.

## The parts I didn't expect to use

A few blades I ignored on the first pass turned out to be the ones I'd
actually keep.

**Live Reports** are dashboards you build by talking to the agent. You
describe what you want, it authors the report, and the report re-queries
live data every time you open it. Each one is versioned and keeps a link
back to the thread that created it, so when the numbers look wrong you can
go read how they were defined instead of reverse-engineering a chart.

<Screenshot
  src={liveReport}
  alt="A live report in Azure SRE Agent titled MCViewer Metrics Dashboard showing KPI tiles for total requests, failed requests, and average duration over 24 hours, above line and bar charts for request count, average request duration, failed requests, and dependency count"
  caption="A dashboard authored by chatting with the agent, versioned, with an Open authoring thread button so you can see how it was built."
/>

**Incidents** is the piece I still haven't connected, and the portal is
blunt about what that costs you: without an incident platform wired up, the
agent handles conversations and scheduled work but doesn't have a queue to
work from. The same blade carries triggers and response plans, which is
where the "respond automatically when this fires" behavior gets defined.

<Screenshot
  src={incidentsOnboarding}
  alt="Incidents page in Azure SRE Agent with tabs for Incidents and Triggers plus response plans, agent status counters for pending user input, in progress, and completed all at zero, and a get started panel prompting to connect an incident platform"
  caption="Incidents, unconnected. The counters and the response-plan tab only start earning their keep once a platform is wired in."
/>

**Agent Canvas** is where the subagent story stops being something Microsoft
does for you and starts being something you build. You can create your own
subagents and skills, wire tools to them, view the whole thing as a graph or
a table, and test it in a playground before it touches anything real.

<Screenshot
  src={agentCanvas}
  alt="Agent Canvas in Azure SRE Agent showing an empty canvas with an illustration of an incident response plan connected to subagents and tools, a Create subagent and Create skill button, and toggles for canvas view, table view, and test playground"
  caption="Agent Canvas. Build your own subagents and skills, then test them in a playground before they run for real."
/>

## How to switch on the parts you don't get by default

Everything above is what the agent does out of the box. The team that builds
it published
[Harness Engineering for Azure SRE Agent](https://techcommunity.microsoft.com/blog/appsonazureblog/the-agent-that-investigates-itself/4500073)
in March, and the headline is counterintuitive: they got better results by
taking scaffolding away. Fewer pre-written queries, fewer hand-built response
plans, more raw access to code and context. Their "Intent Met" score on novel
incidents, meaning the on-call engineer agreed the investigation found the
real root cause, went from 45% to 75%.

Read it if you want the full story. What I care about here is that most of
what made it work is a switch in the portal you already have open, and it
isn't switched on for you. Five of them are worth your afternoon.

### Turn on workspace tools

File operations and terminal commands sit behind a per-agent experimental
flag. Agents created before March 10, 2026 need `EnableWorkspaceTools` under
**Capabilities > Experimental Settings**; newer ones get it by default.

These are toggles, not something the platform stamps once, so two agents in
the same subscription drift apart the moment somebody flips a switch on one
of them. Ask the resource rather than trusting your memory:

```bash
az resource show \
  --resource-group <resource-group> \
  --name <agent-name> \
  --resource-type Microsoft.App/agents \
  --query properties.experimentalSettings -o json
```

### Know what the default sandbox can't do

The article describes the agent installing packages mid-investigation and
opening PRs from the shell. The Code Interpreter you get by default does none
of that. It's an isolated Azure Container Apps session with no outbound
network, no process spawning, no `pip install`, and no filesystem access
outside `/mnt/data/`. Pandas, matplotlib, and seaborn come preinstalled, and
that's the deal.

Workspace tools move Python execution into the workspace sandbox where your
repo lives. That's a different environment, not a permission bump on the same
one. The docs don't say whether `pip` and `apt` come along, so if
"an agent that installs its own tooling" is why the article grabbed you, test
that before you design around it.

One thing that bites people behind a corporate proxy: the sandbox runs under
`*.azuresre.ai`, and some network profiles block that domain. If the portal
loads but code execution hangs, check the allowlist first.

### Let it keep its own notes

Your agent keeps a knowledge directory at `memories/synthesizedKnowledge/`.
It's structured Markdown the agent reads and writes with its normal file
tools, not a vector store you query. An `overview.md` loads into the system
prompt at the start of every conversation and links out to topic files it
reads only when it needs them.

You don't have to wait for it to fill that in. Ask directly in chat:

```text
Save this to your knowledge: Application Insights for the Copilot Studio
agent lives in the <resource-group> resource group, and the dependency
failures we care about come from the Foundry agent, not the app servers.
```

The caveat is Microsoft's, not mine: staleness is unsolved. When a service
changes behavior, old entries turn into confidently wrong context, and they
say plainly that they don't have a systemic fix. Budget time to review what
it wrote, the way you'd review a team wiki.

### Give it the repo

Teams pre-write log queries because they don't trust the agent to generate
correct ones, and that distrust is fair, since models hallucinate table names.
The fix isn't tighter restriction, it's grounding. When the agent can read the
code that produces the logs, it knows which exceptions get thrown and under
what conditions, and it can reason about code paths nobody instrumented,
which are disproportionately the ones involved in novel failures.

That's the Code Access page I set up earlier. What I didn't mention then is
what happens next: the agent reads your project structure, stack, and
dependencies, then opens a PR adding an `SREAGENT.md` file to your repo. Read
that PR properly instead of rubber-stamping it. It's the file that shapes how
every future investigation starts.

### Put a scheduled task on whatever keeps breaking

This is the easiest piece to copy and the one with the clearest payoff. Their
example was mundane: LLM errors were stalling investigations, so they set up
a daily task to cluster the last 24 hours of errors, trace the top offenders
into the codebase, and submit a PR for an engineer to review. Errors dropped
by more than 80% over two weeks.

You build that under **Scheduled tasks > Create task**. Task details is a
prompt, so write it like one:

```text
Query Application Insights for exceptions and failed requests in the last
24 hours across every connected resource. Cluster them by exception type
and failing operation, rank by count, and for the top three trace each one
to the specific file and line in the connected repository. Report what you
found and what you would change. Don't open a PR without asking me first.
```

The field to stop and think about is **Agent autonomy level**. Autonomous is
the default and lets the agent act without approval, which is not where I'd
start. Run it in Review mode, use **Run task now** to watch it once before it
runs unattended, then check **Monitor > Session insights**, where symptoms,
root cause, and pitfalls get pulled out of each thread with a link back to
the conversation.

### What doesn't transfer

Worth naming the ceiling. That post is about how Microsoft engineers its own
harness against its own source, telemetry, and deployment history, and the
numbers come out of that specific setup. What transfers is the shape: give it
your repo, let it keep its own notes, put a scheduled task on whatever keeps
breaking, and stop pre-writing the queries you don't trust it to write.

Their closing line is the one I'd underline. They went from an agent that
follows your playbook to one that writes the next one. Mine isn't there yet.
But every piece that would get it there is a switch in a portal I already had
open.

## Running it from the terminal, and the 403 that lies to you

Here's the part I didn't know existed when I wrote everything above: you don't
need the portal at all. The SRE Agent tools ship inside
[Azure MCP Server](https://learn.microsoft.com/azure/sre-agent/mcp-server), so
any MCP client can drive your agent. GitHub Copilot CLI, VS Code, Cursor, and
Claude Code are all supported. Tools show up prefixed with `sreagent_`.

Listing my agents took one command and about two seconds:

```bash
npx -y @azure/mcp@latest sreagent agents list \
  --subscription <subscription-id>
```

Then I ran a real investigation without opening a browser:

```bash
npx -y @azure/mcp@latest sreagent threads investigate \
  --agent <agent-name> \
  --resource-group <resource-group> \
  --subscription <subscription-id> \
  --message "Read-only check: list the Azure resources you can see and tell me
which ones are reporting an unhealthy or degraded state. Don't modify anything." \
  --max-iterations 4
```

Twenty messages later it had walked about 430 resources across 40-odd resource
groups and come back with a verdict: nothing degraded, two resources reporting
Unknown because they were idle rather than broken. The reasoning steps stream
back as messages with `messageType: Reasoning`, so you watch it think in your
terminal the same way you would in the portal.

Getting there took me longer than the investigation did, and all of it went to
one error message that was wrong in three different ways.

### Owner on the resource buys you nothing

The first data-plane call failed:

```text
status 403: Forbidden: Access denied by PDP
```

I was Owner on that agent. Owner is a control-plane role, and the agent's data
plane, everything served from `*.azuresre.ai`, is gated separately. Four
dedicated roles exist, and none of them come with Owner or Contributor:

```bash
az role definition list --query "[?contains(roleName,'SRE')].roleName" -o tsv
```

That returns SRE Agent Reader, SRE Agent Standard User, SRE Agent Author, and
SRE Agent Administrator. Threads, memories, skills, hooks, scheduled tasks,
and workflows all live behind them. Assign one at the agent resource:

```bash
az role assignment create \
  --assignee <your-upn> \
  --role "SRE Agent Administrator" \
  --scope "/subscriptions/<subscription-id>/resourceGroups/<resource-group>/providers/Microsoft.App/agents/<agent-name>"
```

### The same call, the same identity, two different answers

The role assignment didn't fix it. Ten minutes later I was still getting 403,
so I stopped blaming propagation delay and called the endpoint directly with
the token my CLI was already using:

```bash
az account get-access-token \
  --resource 59f0a04a-b322-4310-adc9-39ac41e9631e \
  --query accessToken -o tsv
```

That's the first-party SRE Agent app ID, and it's the audience the data plane
wants. Not `https://management.azure.com`, which is the same mismatch behind
the 401 people hit on HTTP triggers. With that token, a plain `GET` against
`/api/v2/extendedAgent/skills` on the agent endpoint returned **200**.

So the service was fine and my permissions were fine. The tool was signing in
as somebody else. `DefaultAzureCredential` walks an ordered chain of credential
sources, and on a machine with several Azure sign-ins it doesn't have to land
on the one you used for `az login`. Pin it, in PowerShell:

```powershell
$env:AZURE_TOKEN_CREDENTIALS = "AzureCliCredential"
```

Or in bash:

```bash
export AZURE_TOKEN_CREDENTIALS=AzureCliCredential
```

Same command, same second, 200 and an empty skills list.

### Three layers of misdirection

Worth spelling out, because each one sent me somewhere useless:

The transport wrapper reports **status 503** with a message about service
availability and network connectivity. Nothing was down. Underneath it was a
403, and the 503 sent me debugging the wrong layer, the same trap the private
DNS zones set earlier in this post.

The 403 says "Access denied by PDP." That reads like an authorization problem
with the resource, so you go fix RBAC. RBAC was a genuine prerequisite, but it
wasn't the blocker.

And the agent's own audit log is silent. Successful data-plane calls land in
Application Insights with caller name, status, and the full list of actions the
caller was entitled to. Denied ones never arrive, because the policy decision
happens before the request reaches the agent. Don't waste time querying the
telemetry for your own failures the way I did. Compare a direct `curl` against
the tool instead. That's the test that tells you whether the problem is the
service or the client.

### One flag to be deliberate about

While you're in there, note what else the CLI exposes. Alongside
`threads investigate` there's `threads investigate_yolo`, which runs the same
loop and auto-approves every pending gate. Microsoft's own documentation is
unusually direct about it: there's no read-only restriction, the agent can
invoke any tool its managed identity permits, and that includes deleting pods,
applying Kubernetes YAML, and scaling resources. It's a genuinely useful flag
for a lab subscription. It is not something to reach for against production
because a thread stalled waiting on your approval.

## What the agent writes down about itself

Every agent ships its own telemetry to an Application Insights resource, and
**Monitor > Logs** in the agent portal drops you straight into it. The
documentation tells you to query `customEvents` and lists nine event types. I
ran that query against my own agent expecting to confirm the list:

```kql
customEvents
| where timestamp > ago(30d)
| summarize Count = count() by name
| sort by Count desc
```

Two of the busiest events that came back aren't on the documented list at all.

### An access log nobody mentions

`AgentInboundRequestAudit` fires on every inbound data-plane call, and it's a
proper access log: the path and HTTP method, the response status, the caller's
display name, whether identity resolution succeeded, and the complete list of
`Microsoft.App/agents/*` actions that caller was entitled to. If you need to
answer "who touched this agent, and what were they allowed to do," it's already
being recorded.

With one gap, and it's the one that cost me time earlier: denied calls never
show up. The policy decision happens upstream of the agent, so a 403 leaves no
trace in the agent's own telemetry. Successes are logged, refusals are not.
It's an access log for what happened, not an audit trail of what was blocked.

### The token fields in the docs aren't there

The documented example for tracking spend sums `InputTokens` and `OutputTokens`
off `ModelGeneration` events. Mine returned zeros, so I enumerated every field
name on those events. Neither key exists. Not empty, not renamed that I could
find. Absent.

That matters because it's the obvious way you'd try to reconcile your AAU bill
against actual model usage, and it quietly returns a confident zero rather than
an error. If you build a cost dashboard on that query, check that it's counting
anything at all before you trust it.

### It writes down what the model was thinking

Here's what `ModelGeneration` does carry, on the `ModelGenerationEnd` events:
a `ModelThinking` field holding the model's own reasoning in plain text, along
with `ModelOutput`, `ModelId`, and the reasoning `TaskType`.

Read that twice before you decide how you feel about it. The chain of thought
your agent produced while deciding what to do with your production estate is
sitting in an Application Insights resource, under whatever retention policy
and whatever RBAC that resource happens to have. As a debugging aid it's
excellent, and it's how I confirmed which model actually answers when the agent
is set to Automatic. As a data-governance question it deserves a real answer
before you connect this to anything regulated, and I haven't seen Microsoft
address it anywhere.

### There's a pre-execution gate you can't write to

The last surprise came out of `AgentHookEvaluation`, which logs a decision every
time a tool runs:

```kql
customEvents
| where name == "AgentHookEvaluation"
| extend Event = tostring(customDimensions.HookEventType),
    Tool = tostring(customDimensions.ToolName),
    Decision = tostring(customDimensions.FinalDecision),
    SystemHooks = tostring(customDimensions.SystemHooksRun),
    UserHooks = tostring(customDimensions.UserGlobalHooksRun)
| summarize Count = count() by Event, Tool, Decision, SystemHooks, UserHooks
```

Mine returns two event types: `PreToolUse` and `PostToolUse`, each with system
hooks running and zero user hooks, every decision `pass`.

The [hooks documentation](https://learn.microsoft.com/azure/sre-agent/agent-hooks)
says two hook events are currently supported, and lists **Stop** and
**PostToolUse**. `PreToolUse` isn't among them. So the runtime evaluates a gate
before a tool executes, but the hooks you're invited to author run either after
the tool already ran or when the agent tries to finish.

That's a real distinction and not a pedantic one. A `PostToolUse` hook can
block a *result*, inject context, and tell the agent to think again. What it
cannot do is stop the command from running in the first place. The pre-execution
veto exists, it's enforcing something on every call, and right now it's
Microsoft's, not yours. Worth knowing before you promise anyone that hooks will
prevent a destructive command.

Hooks are still the most interesting governance surface here. Stop hooks let you
define what "done" means and force the agent to keep working, command hooks run
sandboxed bash or Python with a 64 KB script limit, and a rejection without a
`reason` field is silently treated as approval. That last one will bite someone.

## What the hour actually cost

Worth knowing before you turn this on: billing runs on Azure agent units, or
AAUs, and it splits into two parts. There's a fixed always-on charge for
having the agent running at all, and a variable charge for active work, which
covers chats, incidents, scheduled tasks, and triggers. You set the
active-flow allocation yourself and can raise or lower it whenever you want.

My hour of poking at it burned 138 of the 10,000 active-flow AAUs I'd
allocated for the month. Chats accounted for 127 of those and triggers for the
other 11. That's roughly one percent of the allocation for a full repo review
plus a telemetry sweep across five services, which is a useful anchor if
you're trying to guess what a real on-call rotation would run.

<Screenshot
  src={agentConsumption}
  alt="Azure SRE Agent Consumption page showing a monthly limit of always-on flow plus 10,000 active flow AAUs, with 138 of 10,000 consumed for August 2026 broken down as 127 from chats and 11 from triggers, above a daily consumption bar chart"
  caption="The Agent Consumption page breaks usage down by source, so you can see what's actually spending your allocation."
/>

## Why this matters if you admin Azure for a living

Cloud admins already know the shape of the problem this addresses: you're
watching more services than you have hours to check by hand, alerts fire
faster than root causes get found, and the person who understands why a
system behaves the way it does is usually one person, not a team. Azure SRE
Agent's pitch is that every investigation it runs builds institutional
knowledge that sticks around even when that one person is on vacation or
moves to another team.

That pitch isn't just theoretical. In
[Rethinking cloud operations with Azure SRE Agent](https://techcommunity.microsoft.com/blog/appsonazureblog/a-paradigm-shift-in-cloud-operations-with-azure-sre-agent/4533244),
Microsoft walked through what happened when three customers actually put it
into production. Zafin took incident triage from hours down to minutes.
Provation Medical had the agent produce analysis for every production-related
ticket in its first month. InEight reported an 80 percent drop in incident
investigation time, an 80 percent drop in build-failure triage time, and a 67
percent drop in bug investigation time. None of those are demo numbers; they're
what happened after the agent was wired into real on-call rotations.

It's not magic, and it's not a reason to stop reviewing what it recommends.
Microsoft is upfront that, like any AI system, it can produce an incorrect
root cause or propose a mitigation that doesn't fit your environment. Treat
its output the way you'd treat a report from a sharp junior engineer: worth
taking seriously, still worth checking before you act on it.

## Go look at what you already have running

If you already have Application Insights collecting telemetry from more than
one service, that's the fastest way to find out whether this is worth your
time. Connect those resources, ask the agent what's unhealthy, and compare
what it says to what you thought was true. An hour is enough to get an answer,
and it's a better hour than the one you'll spend correlating dashboards during
your next on-call rotation.
